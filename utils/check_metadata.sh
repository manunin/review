#!/bin/bash
# Утилита для проверки метаданных SQLAlchemy.
#
# Автоматически активирует виртуальное окружение и запускает скрипт проверки.

# Определяем корень проекта
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "🚀 Запуск проверки метаданных SQLAlchemy..."
echo "📁 Проект: $PROJECT_ROOT"

# Переходим в корень проекта
cd "$PROJECT_ROOT"

# Проверяем наличие виртуального окружения
if [ ! -d "venv" ]; then
    echo "❌ Виртуальное окружение 'venv' не найдено!"
    echo "   Создайте его командой: python -m venv venv"
    exit 1
fi

# Активируем виртуальное окружение и запускаем скрипт
echo "🔧 Активируем виртуальное окружение..."
source venv/bin/activate

echo "▶️  Запускаем проверку..."
python utils/check_metadata.py

echo ""
echo "✅ Готово! Теперь вы знаете, как работает импорт моделей в Alembic."
