# JavaScript to TypeScript Migration Plan

## 📦 Этап 1: Подготовка инфраструктуры

1. **Добавить зависимости**
   - Установить TypeScript: `npm install --save-dev typescript`
   - Установить типы для Vue: `npm install --save-dev @types/node @vue/tsconfig`
   - Установить vue-tsc для типизации компонентов: `npm install --save-dev vue-tsc`
   - Установить типы для сторонних библиотек (например, PrimeVue, Pinia, Chart.js): `npm install --save-dev @types/chart.js`

2. **Инициализация TypeScript**
   - Сгенерировать `tsconfig.json` с поддержкой Vue 3 и Vite.
   - Добавить конфиг для `vue-tsc` (например, в package.json или отдельный файл).

3. **Обновить Vite конфиг**
   - Добавить поддержку TypeScript (Vite поддерживает .ts/.vue из коробки, но стоит проверить плагины).

---

## 🗂️ Этап 2: Миграция файлов

4. **Переименование файлов**
   - Переименовать все `.js` файлы в `.ts` (компосаблы, сервисы, утилиты).

5. **Миграция компонентов**
   - В каждом компоненте заменить `<script>` на `<script lang="ts">`.

## 🛠️ Этап 3: Типизация и рефакторинг

6. **Типизация props и emits**
   - В каждом компоненте явно описать типы props и emits.
   - Для generic компонентов использовать generic type parameters.

7. **Типизация Pinia store**
   - Описать интерфейсы для состояния store.
   - Типизировать actions, getters, возвращаемые значения.

8. **Типизация сервисного слоя**
   - Описать интерфейсы для всех API-ответов.
   - Типизировать параметры и возвращаемые значения функций.

9. **Типизация composables**
    - Все composables должны возвращать строго типизированные объекты.
    - Использовать типы для параметров и возвращаемых значений.

---

## 🧪 Этап 4: Проверка и настройка

10. **Настроить vue-tsc для проверки типов**
    - Добавить скрипт в package.json: `"type-check": "vue-tsc --noEmit"`
    - Запустить проверку типов и устранить ошибки.


## 🏁 Этап 5: Финальная проверка

11. **Запустить проект**
    - Проверить отсутствие ошибок типизации.
    - Убедиться в корректной работе приложения.

12. **Провести ревью**
    - Проверить, что все props, emits, сервисы, composables типизированы.
    - Убедиться, что нет any/unknown без необходимости.

---

### 💡 Рекомендации

- Использовать строгий режим TypeScript (`strict: true` в tsconfig).
- Не использовать `any` без крайней необходимости.
- Для сложных структур использовать отдельные интерфейсы.
- Для API-ответов создавать типы, соответствующие реальным данным.
- Для composables использовать типы с generic параметрами, если требуется универсальность.
- Все компоненты должны быть типизированы, включая props, emits, slots.